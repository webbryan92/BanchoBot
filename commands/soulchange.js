const Discord = require("discord.js");

module.exports.run = async (bot, message, args) => {
    /*guild = messgage.guild
    //prototyping
    bot.guilds.get(guild.id)

    method soulchange(){
        case switch
            banchosoul > 80
                //do for every case
                //remove all other potential roles
                removerole()
                //add main role
                addrole()
            banchosoul > 60
            banchosoul > 40
            banchosoul > 20
            banchosoul <= 20
    }
    if(args = good mood){
        banchosoul++
        soulChange()
    }
    else{
        banchosoul--
        soulChange()
    }*/
}

module.exports.help = {
    name: "soulchange"
}